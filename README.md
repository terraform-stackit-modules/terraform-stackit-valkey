<!-- BEGIN_TF_DOCS -->
# Terraform STACKIT Valkey module

Terraform module which provisions a [STACKIT Valkey](https://registry.terraform.io/providers/stackitcloud/stackit/latest/docs/resources/valkey_instance) instance and its credentials on [STACKIT](https://www.stackit.de/en/).

Valkey is the successor to STACKIT's Redis offering (the `stackit_redis_*` resources are deprecated and will be removed after August 2027). Prefer this module over `terraform-stackit-redis` for new workloads.

This repository is not from the official STACKIT organization.

## Usage

```hcl
module "valkey" {
  source  = "terraform-stackit-modules/valkey/stackit"
  version = ">= 1.0.0"

  project_id     = "xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx"
  name           = "app-valkey"
  valkey_version = "7"
  plan_name      = "stackit-valkey-single-4"

  parameters = {
    sgw_acl          = "0.0.0.0/0"
    maxmemory_policy = "volatile-lru"
  }

  credentials = {
    app = {}
  }
}
```

See [`examples/basic`](./examples/basic) for a self-contained example (requires only `project_id`).

## Composite module

- `modules/instance` — the `stackit_valkey_instance` (toggled by `create_instance`).
- `modules/credential` — `stackit_valkey_credential` entries (`for_each` over `credentials`).

The root wires them together and, when `create_instance = false`, targets an existing instance via
`instance_id` for credential management.

## Notes

- Generated credentials (`password`, `uri`) are exposed via **sensitive** outputs.
- `credentials` is a map keyed by a **stable identifier**; the instance\_id (known after apply when
  the instance is created here) is only a resource attribute, never a `for_each` key.
- Valkey (like Redis) has no separate database/user resources — access is via credentials
  (auto-generated username/password).

<!-- markdownlint-disable MD001 -->
### Contributing

This module follows the conventions of the `terraform-stackit-modules` organization. Before opening a PR:

1. [Install pre-commit](https://pre-commit.com/#install) and run `pre-commit install`.
2. Install the required tools: [tflint](https://github.com/terraform-linters/tflint), [tfsec](https://aquasecurity.github.io/tfsec/), [terraform-docs](https://github.com/terraform-docs/terraform-docs), [golang](https://go.dev/doc/install), [coreutils](https://www.gnu.org/software/coreutils/).
3. Run the checks: `pre-commit run -a`.

**Do not manually edit `README.md`** — it is generated by `terraform-docs` from this file and the module's inputs/outputs.

## Terratest

The `test/` directory holds a Terratest integration test that applies the `examples/basic` root module against a real STACKIT project (requires `STACKIT_SERVICE_ACCOUNT_KEY` and `STACKIT_PROJECT_ID`).

```bash
cd test
go mod init github.com/terraform-stackit-modules/terraform-stackit-valkey
go get github.com/gruntwork-io/terratest@v1.0.1
go mod tidy
go test -v -timeout 45m ./...
```

## Requirements

| Name | Version |
| ---- | ------- |
| <a name="requirement_terraform"></a> [terraform](#requirement\_terraform) | >= 1.3 |
| <a name="requirement_stackit"></a> [stackit](#requirement\_stackit) | >= 0.113.0 |

## Providers

No providers.

## Modules

| Name | Source | Version |
| ---- | ------ | ------- |
| <a name="module_credential"></a> [credential](#module\_credential) | ./modules/credential | n/a |
| <a name="module_instance"></a> [instance](#module\_instance) | ./modules/instance | n/a |

## Resources

No resources.

## Inputs

| Name | Description | Type | Default | Required |
| ---- | ----------- | ---- | ------- | :------: |
| <a name="input_project_id"></a> [project\_id](#input\_project\_id) | STACKIT project ID to which the Valkey instance and its credentials are associated. | `string` | n/a | yes |
| <a name="input_create_instance"></a> [create\_instance](#input\_create\_instance) | Whether to create the Valkey instance. Set to false to manage credentials against an existing instance provided via `instance_id`. | `bool` | `true` | no |
| <a name="input_credentials"></a> [credentials](#input\_credentials) | Map of credentials to create on the instance, keyed by a stable identifier. Each value:<br/>  - `rotate_when_changed` : optional map whose change forces credential rotation.<br/>Generated passwords/URIs are exposed via the `credential_passwords` / `credential_uris`<br/>outputs (sensitive). | <pre>map(object({<br/>    rotate_when_changed = optional(map(string))<br/>  }))</pre> | `{}` | no |
| <a name="input_instance_id"></a> [instance\_id](#input\_instance\_id) | ID of an existing Valkey instance. Used for credentials when `create_instance` is false. | `string` | `null` | no |
| <a name="input_name"></a> [name](#input\_name) | Instance name. | `string` | `null` | no |
| <a name="input_parameters"></a> [parameters](#input\_parameters) | Optional instance configuration parameters (all optional):<br/>`sgw_acl`, `enable_monitoring`, `monitoring_instance_id`, `graphite`, `metrics_frequency`,<br/>`metrics_prefix`, `max_disk_threshold`, `maxclients`, `maxmemory_policy`, `syslog`. | <pre>object({<br/>    sgw_acl                = optional(string)<br/>    enable_monitoring      = optional(bool)<br/>    monitoring_instance_id = optional(string)<br/>    graphite               = optional(string)<br/>    metrics_frequency      = optional(number)<br/>    metrics_prefix         = optional(string)<br/>    max_disk_threshold     = optional(number)<br/>    maxclients             = optional(number)<br/>    maxmemory_policy       = optional(string)<br/>    syslog                 = optional(list(string))<br/>  })</pre> | `null` | no |
| <a name="input_plan_name"></a> [plan\_name](#input\_plan\_name) | The selected plan name. | `string` | `null` | no |
| <a name="input_region"></a> [region](#input\_region) | The resource region. If not defined, the provider region is used. | `string` | `null` | no |
| <a name="input_valkey_version"></a> [valkey\_version](#input\_valkey\_version) | The Valkey service version, e.g. "7". | `string` | `null` | no |

## Outputs

| Name | Description |
| ---- | ----------- |
| <a name="output_credential_ids"></a> [credential\_ids](#output\_credential\_ids) | Map of credential key to credential ID. |
| <a name="output_credential_passwords"></a> [credential\_passwords](#output\_credential\_passwords) | Map of credential key to generated password. Sensitive. |
| <a name="output_credential_uris"></a> [credential\_uris](#output\_credential\_uris) | Map of credential key to connection URI. Sensitive. |
| <a name="output_credential_usernames"></a> [credential\_usernames](#output\_credential\_usernames) | Map of credential key to generated username. |
| <a name="output_dashboard_url"></a> [dashboard\_url](#output\_dashboard\_url) | The dashboard URL of the instance. |
| <a name="output_instance_id"></a> [instance\_id](#output\_instance\_id) | The ID of the Valkey instance (created, or the provided instance\_id when create\_instance is false). |
| <a name="output_plan_id"></a> [plan\_id](#output\_plan\_id) | The resolved plan ID of the instance. |
<!-- END_TF_DOCS -->
